import React from 'react';
import PersonCard from "./PersonCard";

export const PersonCardPage = props => {
    // console.log(props,"props stuff");

    return (
        <div>
        {/* map through data here
        {props.cardData.map((post,index) => {
          return <PersonCard key={index} post={post}/>
        })}; */}
  
      </div>

    );    
};



export default PersonCardPage
